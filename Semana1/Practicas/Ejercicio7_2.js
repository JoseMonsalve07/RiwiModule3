// Parte 3: Combinación de Operaciones y Concatenación Compleja

// Historia de Usuario 7: Monitoreo de Estado de Servidor

let servidor = "Web_Server_01";
let status = "Activo";
let pingMs = 25;
let mensajeEstado = `El servidor ${servidor} está ${status} con una latencia de ${pingMs}ms`;

console.log(mensajeEstado);